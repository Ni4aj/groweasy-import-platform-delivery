import './globals.css';

export const metadata = { title: 'GrowEasy Import Studio', description: 'AI-assisted CRM CSV standardization' };

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
